/*
  === COMPILING INSTRUCTIONS ===
  Compile all Java files:
  javac -cp "Calculator/lib/*" -d bin $(find Calculator/src -name "*.java")

  Run any class:
  java -cp "bin:Calculator/lib/*" calculator.ClassName

  Example:
  java -cp "bin:Calculator/lib/*" calculator.FractionTest
*/
