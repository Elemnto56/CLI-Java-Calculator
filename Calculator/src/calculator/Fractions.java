package calculator;
import org.apache.commons.math3.fraction.*;

public class Fractions {
    static Fraction frac = new Fraction(5, 10);
    public static void main(String[] args) {
        System.out.println(frac);
    }
}
